pipeline {
    agent any

    stages {
        stage('Get Branch Name') {
            steps {
                script {
                    def branchName = env.GIT_BRANCH
                    echo "Current branch name: ${branchName}"
                }
            }
        }
    }
}